import ShaderSketchBase from './ShaderSketchBase';

export default class ExampleShaderSketch extends ShaderSketchBase {
  constructor(canvas, fragmentShader, vertexShader) {
    super(canvas, fragmentShader, vertexShader);
  }

  update(time) {
    super.update(time);
  }
}